spring.application.name=middleware

# ==========================================
# RABBITMQ CONFIGURATION
# ==========================================
# Connects dynamically to CloudAMQP or uses your fallback local/cloud URL
spring.rabbitmq.addresses=${RABBITMQ_URL:amqps://xonssbey:nkHeBNRYjIfn3PPDuWy9iFM7BCIYoyCa@horse.lmq.cloudamqp.com/xonssbey}

# Application Custom Queue Settings matching RabbitMQConfig.java
openfloat.rabbitmq.exchange=openfloat.erp.exchange
openfloat.rabbitmq.queue=openfloat.erp.queue
openfloat.rabbitmq.routing-key=erp.payment.success

# RabbitMQ Exponential Backoff & DLQ Policy
spring.rabbitmq.listener.simple.retry.enabled=true
spring.rabbitmq.listener.simple.retry.initial-interval=2000
spring.rabbitmq.listener.simple.retry.multiplier=2.0
spring.rabbitmq.listener.simple.retry.max-attempts=3
spring.rabbitmq.listener.simple.default-requeue-rejected=false

# ==========================================
# DATASOURCE & PERSISTENCE (POSTGRESQL)
# ==========================================
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d9fgo3741pts73e5pui0-a.oregon-postgres.render.com/openfloat_db?sslmode=require}
spring.datasource.username=${DB_USERNAME:openfloat_db_user}
spring.datasource.password=${DB_PASSWORD:WUWGwK6ZpcjhXGn9smIgMFyUu0pbXJXX}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Dialect specifically for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Auto-create/update tables
spring.jpa.hibernate.ddl-auto=update

# Disable H2 Console
spring.h2.console.enabled=false



# ==========================================
# SECURITY & ADMIN CREDENTIALS
# ==========================================
ADMIN_USERNAME=${ADMIN_USERNAME}
ADMIN_PASSWORD=${ADMIN_PASSWORD}
# ==========================================
# SAFARICOM DARAJA API SETTINGS
# ===================================
safaricom.daraja.consumer-key=${DARAJA_CONSUMER_KEY:your_local_key}
safaricom.daraja.consumer-secret=${DARAJA_CONSUMER_SECRET:your_local_secret}
safaricom.daraja.shortcode=${DARAJA_SHORTCODE:174379}
safaricom.daraja.passkey=${DARAJA_PASSKEY:your_local_passkey}
safaricom.daraja.stk-push-url=${DARAJA_STK_PUSH_URL:https://sandbox.safaricom.co.ke/mpesa/stkpush/v1/processrequest}
safaricom.daraja.callback-url=${DARAJA_CALLBACK_URL:https://openfloat.onrender.com/api/v1/payments/callback}
safaricom.daraja.auth-url=${DARAJA_AUTH_URL:https://sandbox.safaricom.co.ke/oauth/v1/generate?grant_type=client_credentials}

# Safaricom B2C Configuration
safaricom.b2c.initiator-name=${SAFARICOM_B2C_INITIATOR_NAME:testapi}
safaricom.b2c.initiator-password=${SAFARICOM_B2C_INITIATOR_PASSWORD}
safaricom.b2c.shortcode=${SAFARICOM_B2C_SHORTCODE:600996}
safaricom.b2c.result-url=${LIVE_API_URL}/api/v1/b2c/result
safaricom.b2c.timeout-url=${LIVE_API_URL}/api/v1/b2c/timeout

safaricom.consumer-key=${SAFARICOM_CONSUMER_KEY}
safaricom.consumer-secret=${SAFARICOM_CONSUMER_SECRET}